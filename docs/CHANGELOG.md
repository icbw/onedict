# 更新日志

本文件记录公开发布版本。每次发布以单次提交更新本仓库，并打 `vX.Y.Z` 标签。

## v0.1.17 — 2026-09-15

**本次更新**

- 版本更新测试

## v0.1.16 — 2026-09-15

**新增**

- **应用内更新**：设置页新增「关于」子页——检查更新、下载新版本（显示进度）、确认后安装并自动重启；
  更新包经签名校验，校验不通过不会安装。可开启「启动时检查更新」（默认关闭）。
- **开机启动**：设置页「通用」子页新增开关（默认关闭）——登录 Windows 后自动启动并静默驻留托盘。

**说明**

- 自动更新自本版开始生效：v0.1.15 及更早版本需手动安装本版一次。
- 更新不影响本地数据：词典引用、生词本、偏好与查词历史均保留。

**改进**

- 便携模式（exe 同目录放置 `onedict.portable`）下不提供在线更新，避免便携副本被装成安装版。

## v0.1.15 — 2026-09-10（首次公开发布）

**修复**

- 词典页搜索历史在冷启动后需先查询一次才显示（启动时序竞态；历史数据本身未丢失）
- 翻译页可能停留「未配置模型服务」空态（启动早期读取失败后不再自愈）
- 划词动作栏显示的动作数量与设置页不一致

**改进**

- 偏好与本地数据初始化提前到窗口创建之前，启动期读取更稳定
- 翻译页可选任意已配置服务商的模型（跨服务商路由）

## 开发期版本（0.1.1 – 0.1.14）

首次公开发布前的内部迭代，仅列主要变化：

| 版本 | 主要变化 |
|---|---|
| 0.1.14 | 应用图标定稿（透明底板：书本 + 放大镜渐变） |
| 0.1.13 | 应用图标更新（第一版设计） |
| 0.1.12 | 启动期偏好读取竞态修复（划词动作数量错乱） |
| 0.1.11 | 翻译页模型断链修复；设置窗文案简化 |
| 0.1.9 – 0.1.10 | 截图翻译批：AI 回填对齐、截图默认不自动识别可拆分、AI OCR 与系统 OCR 解耦 |
| 0.1.8 | 视觉 OCR 模型输出适配（结构化 JSON 与非流式输出兜底） |
| 0.1.7 | 界面组件库与主题令牌自研化（强调色、弹窗圆角调整）；依赖面收缩 |
| 0.1.6 | 设置页模型服务首启空态根治 |
| 0.1.5 | 模型卡改版（侧边扩展池 + 逐模型能力标记） |
| 0.1.1 – 0.1.4 | 安装包分发硬化：CSP 修复、数据目录分层（正式版 / 开发版 / 便携）、词典管理、备份容器 v2 |
