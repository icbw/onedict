# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.85"
name = "opendict-rs"
version = "0.1.0"
authors = ["Callum Gander <callumgander@protonmail.com>"]
build = false
exclude = [
    "/expo",
    "/node",
    "/mobile",
    "/examples/expo-test",
    "/examples/node-test",
    "/scripts",
    "/docs",
    "/tests/dicts",
    "/.github",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Unified Rust reader for StarDict and MDict dictionaries"
homepage = "https://github.com/callum-gander/opendict-rs"
documentation = "https://docs.rs/opendict-rs"
readme = "README.md"
keywords = [
    "stardict",
    "mdict",
    "dictionary",
]
categories = [
    "parser-implementations",
    "text-processing",
]
license = "MIT"
repository = "https://github.com/callum-gander/opendict-rs"

[lib]
name = "opendict"
path = "src/lib.rs"

# vendored by onedict（2026-09-01）：移除上游 examples/benches/tests 目标声明与
# dev-dependencies（criterion/tempfile）——纯 lib crate，内嵌单元测试保留。
# 修改内容见 ONEDICT-NOTES.md。

[dependencies.adler2]
version = "2"

[dependencies.encoding_rs]
version = "0.8"

[dependencies.flate2]
version = "1"

[dependencies.log]
version = "0.4"

[dependencies.lzo1x]
version = "0.2"

[dependencies.memmap2]
version = "0.9"

[dependencies.xxhash-rust]
version = "0.8"
features = ["xxh64"]

[dev-dependencies.tempfile]
version = "3"
